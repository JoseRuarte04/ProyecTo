# PROJECT_STATE — HisTO

> Este archivo es la ÚNICA fuente de verdad sobre "dónde estamos ahora".
> Se actualiza al FINAL de cada sesión de trabajo (con Claude Code o sin él).
> Si algo no está acá, no pasó — o no está confirmado.

**Última actualización:** 2026-09-18
**Sprint / objetivo actual:** Ninguno activo — ver Pausado

---

## 🎯 Objetivo de esta semana
Nada en curso ahora mismo. El catálogo HEP2go sigue pausado (falta que Jose corra las migraciones A/B/C de `docs/PLAN_catalogo_ejercicios_hep2go.md`) y Recordatorios de turnos sigue pausado desde julio.

---

## 🟢 En progreso AHORA (máximo 1-2 ítems)
| Tarea | Estado | Bloqueado por | Próximo paso concreto |
|---|---|---|---|
| _(nada en progreso)_ | — | — | — |

Regla: si hay más de 2 filas acá, es mentira — elegí una y pausá el resto explícitamente abajo.

## ⏸️ Pausado (con motivo — esto es lo que normalmente se pierde)
| Tarea | Por qué se pausó | Qué falta para retomarla |
|---|---|---|
| Catálogo global de ejercicios HEP2go | Jose pidió priorizar la revisión y el merge de los PRs de Javito (2026-09-18) | Correr las 3 migraciones pendientes (`20260823100000`/`110000`/`120000`, ver `docs/PLAN_catalogo_ejercicios_hep2go.md`) — confirmado contra Supabase que `exercise_library` todavía no tiene las columnas de catálogo, así que el plan sigue vigente tal cual |
| Recordatorios de turnos | Jose priorizó otra cosa (2026-07-16); el plan quedó completo | Nada — ejecutar `docs/PLAN_recordatorios_turnos.md` de punta a punta (6 commits planificados) |

## ✅ Cerrado esta semana
- **[2026-09-18] Revisión, saneamiento y merge de los 6 PRs abiertos de Javito** (pedido directo de Jose: revisión a fondo primero, después destrabar con la migración drift confirmada): revisión encontró que #15→#16→#17 eran ramas apiladas (cada una contenía todos los commits de la anterior) y que #13/#17 (tipo de documento) y #14/#18 (obra social nombre completo) eran pares de trabajo duplicado. Jose confirmó contra Supabase real que `patients.document_type` y `obras_sociales.full_name` ya estaban aplicados en producción sin el commit correspondiente mergeado a `main`. Trabajo hecho, en orden:
  1. Blindadas las migraciones de #17 y #14 con `IF NOT EXISTS` / `DROP...IF EXISTS` (columnas, constraint y policy ya existían en producción).
  2. Rebaseadas #16 y #17 sobre `main` una vez mergeada cada base de la cadena, para que cada PR quedara solo con sus commits propios (se avisó en cada PR antes del force-push).
  3. Agregado índice único case-insensitive `obras_sociales_name_lower_idx` antes de mergear #14, para evitar duplicados al agregar una obra social nueva desde la UI (ver DECISIONS).
  4. Mergeados en orden: #15 (infraestructura de "cambios sin guardar" + rollout a ~24 formularios), #16 (Editar ficha al header + recorte de campos), #17 (tipo de documento), #14 (obra social con nombre completo + alta desde la UI).
  5. Cerrados sin mergear #13 (superado por #17) y #18 (superado por #14), con comentario y link al reemplazo en cada uno.
  6. PR #19 (nuevo, mergeado): sincroniza el repo con el backfill de `full_name` de 30 obras sociales que traía #18 — ese dato también ya estaba en producción sin commit correspondiente.
  Verificado: typecheck y build limpios en cada rama antes de mergear, CI verde en los 6 PRs+el nuevo. Ningún merge tocó código nuevo sin pasar por review de este mismo hilo. Hallazgo de gobernanza documentado en DECISIONS.md — ver ahí antes de aplicar una migración directo contra Supabase de nuevo.
- **[2026-09-16] Editar ficha: botón al header + formulario recortado a los campos del alta** (pedido directo de Javito, ver DECISIONS): el botón "Editar ficha" pasa de la pestaña Ficha Clínica a un ícono junto al nombre del paciente en el header de `PatientProfile.tsx` (visible desde cualquier pestaña). El diálogo se recorta a los campos del alta de paciente; Perfil ocupacional se saca (se sigue editando en el paso de Admisión de la sesión). Antes de borrar campos clínicos se investigó si tenían otra vía de edición: fecha de lesión/cirugía, mecanismo, tipo de tratamiento, estudios, antecedentes, inmovilización y tratamiento farmacológico se pueden seguir editando reabriendo la sesión de Admisión ("Editar sesión" en la pestaña Sesiones), así que se sacaron del diálogo sin pérdida. Inicio de síntomas, tratamiento actual, próximo turno OyT y notas clínicas no tenían ninguna otra vía — se borraron del formulario y de la vista de solo lectura de Ficha Clínica con aprobación explícita; quedan como columnas deprecadas. Verificado en el navegador contra Supabase real (paciente Ruarte, Javier) — 0 errores de consola. Typecheck y lint sin problemas nuevos. PR: [#16](https://github.com/JoseRuarte04/ProyecTo/pull/16) (mergeado 2026-09-18, rebaseado antes de mergear).
- **[2026-09-15] Borrado el código muerto de "Plan de tratamiento"** (candidato dejado por la sesión anterior, Jose pidió resolverlo): se confirmó que era código muerto de punta a punta — ni `PlanCardActions` (el componente que abría los diálogos) se usaba en ningún lado, y `exportPlanPdf` (`PlanPdfExport.tsx`) solo lo llamaba ese componente muerto. Se borraron `PlanDialogs.tsx` y `PlanPdfExport.tsx` enteros, y en `PatientProfile.tsx` el import, los 4 estados de diálogo y las 2 queries a `treatment_plans` (tampoco se mostraban en ningún lado). La tabla queda en la base sin borrar (0 filas en producción, pero `Exercises.tsx` la usa como guard antes de borrar un ejercicio — no se puede tirar sin revisar eso primero). Verificado en el navegador (Ficha, Evolución, Ejercicios de un paciente real) — 0 errores de consola. Lint bajó de 235 a 207. Typecheck y build OK. Se detectó y corrigió a tiempo un descuido propio: había quedado trabajando directo sobre `main`, se movió a una rama nueva antes de commitear nada. PR: [#11](https://github.com/JoseRuarte04/ProyecTo/pull/11) (mergeado).
- **[2026-09-15] Card vacía de Perfil ocupacional + últimos ~16 sitios de queries sin chequear error** (dos candidatos chicos de TASKS.md, Jose pidió arrancarlos después de que le expliqué de qué se trataban): `SessionForm.tsx` ya no inserta una fila vacía en `patient_occupational_profiles` si el usuario no tocó ese paso del wizard; `FichaTab.tsx` no muestra la card salvo que tenga algún dato. Cerrados los ~16 sitios restantes de queries sin chequear error: autocompletes (CIE-10, obras sociales) y prefills de sesión con `console.error`; dos casos de riesgo real corregidos — `NewEpisodeDialog.tsx` (podía crear un episodio sin diagnósticos guardados mostrando éxito) y `PlanDialogs.tsx` (un plan de tratamiento podía guardarse con 0 ejercicios). Al verificar se encontró que `PlanDialogs.tsx` (Plan de tratamiento, tabla `treatment_plans`) es código muerto, sin ningún botón que lo abra en la UI actual — anotado en TASKS.md, resuelto en la entrada de arriba. Verificado de punta a punta en el navegador contra Supabase real con un paciente de prueba (creado y borrado al final) — 0 errores de consola. Typecheck, lint (235, bajo el techo de 239) y build OK. PR: [#10](https://github.com/JoseRuarte04/ProyecTo/pull/10) (mergeado).
- **[2026-09-15] QA completa de la app + fix de queries con error silencioso** (pedido directo de Jose tras pedirme un pronóstico del estado de la app, ver DECISIONS): pasada de QA manual de toda la app (fork dedicado) — 0 errores de consola en todo el recorrido, único hallazgo real fue una card de Perfil ocupacional que queda vacía en blanco (candidato en TASKS.md, no arreglado). Se retomó el hallazgo de julio "14 queries ignoran el error de Supabase en silencio" — el código creció desde entonces a ~30 sitios, se priorizaron los de mayor impacto real (no los 30 a ciegas): `fetchEpisodeDiagnoses` tenía riesgo real de pérdida de datos (un guardado podía borrar diagnósticos existentes si la carga fallaba en silencio, ahora tira el error y el guardado no toca los diagnósticos si la carga falló), la carga principal de la ficha del paciente (~11 queries sin chequear en un solo fetch), el PDF del plan de ejercicios (se generaba "exitosamente" pero vacío), la consulta de turnos ocupados del día (riesgo de doble turno), y varias vistas/contextos/hooks más — 14 archivos tocados. Quedan ~16 sitios de menor riesgo sin tocar, documentados en TASKS.md. Verificado de punta a punta en el navegador contra Supabase real (Dashboard, ficha con diagnósticos reales, Editar ficha, Nuevo turno, Biblioteca de ejercicios, edición de sesión existente) — 0 errores de consola. Typecheck, lint (239, en el techo) y build OK. PR: [#9](https://github.com/JoseRuarte04/ProyecTo/pull/9) (mergeado).
- **[2026-09-15] Barthel con opciones visibles + Evaluación analítica en acordeón** (a Jose le gustó el patrón de la nueva Evaluación funcional y pidió llevarlo a estos dos lugares, ver DECISIONS): Índice de Barthel reemplaza el `<select>` oculto por botones con todas las opciones visibles (2-4 según el ítem, `BarthelItemToggle` nuevo). Evaluación analítica: las 8 sub-secciones dejan de "activarse" con un switch y pasan a ser un acordeón siempre visible, colapsado por defecto (mismo patrón `Collapsible` que Ocupaciones). Se verificó que los switches viejos eran puramente visuales — el cambio no tocó el guardado de datos; `sections_config` queda deprecada sin borrar. FIM queda con el select de siempre (opciones con etiquetas largas × 18 ítems se verían muy cargadas como botones) — candidato en `TASKS.md` con idea de diseño más compacta. Sin migración SQL. Verificado de punta a punta en el navegador contra Supabase real — 0 errores de consola. Typecheck, lint (235, bajó del techo de 239) y build OK. PR: [#8](https://github.com/JoseRuarte04/ProyecTo/pull/8) (mergeado).
- **[2026-09-15] Perfil ocupacional recortado + Evaluación funcional en 5 apartados (AOTA/MOHO)** (pedido directo de Jose, ver DECISIONS): Perfil ocupacional recortado a 5 campos (dominancia, estado civil, nivel educativo, trabajo, red de apoyo) en Ficha del paciente y sesión de Admisión (se mantienen ambos formularios por decisión explícita). Evaluación funcional rediseñada: checklist de 47 ítems en 9 categorías (AVD/AIVD/Gestión de la salud/Descanso y sueño/Educación/Trabajo/Juego/Ocio/Participación social) calificables independiente/requiere asistencia/dependiente, más 4 apartados de texto libre (Contextos/Patrones de desempeño/Habilidades de desempeño/Factores del cliente); Barthel y FIM se mantienen aparte. Sigue apareciendo en cada sesión (admisión y seguimiento), decisión confirmada con Jose. 2 migraciones aplicadas contra Supabase (14 columnas nuevas en `functional_evaluations`) y `types.ts` regenerado. Se corrigió de paso un bug de doble escritura de AVD/AIVD entre `functional_evaluations` y `patient_occupational_profiles`. Verificado de punta a punta en el navegador contra Supabase real (sesión de Admisión completa, vista de detalle, reapertura de sesión) — 0 errores de consola. Typecheck, lint (239, en el techo) y build OK. 2 PRs: [#6](https://github.com/JoseRuarte04/ProyecTo/pull/6) Perfil ocupacional (mergeado), [#7](https://github.com/JoseRuarte04/ProyecTo/pull/7) Evaluación funcional (mergeado). `NewFuncEvalDialog.tsx` (creación standalone de eval. funcional) queda con un problema preexistente sin resolver (candidato en TASKS.md).
- **[2026-09-15] Pacientes: nacionalidad obligatoria, Sexo, contacto de emergencia separado, alergias** (pedido directo de Jose, ver DECISIONS): nacionalidad obligatoria en el alta (y ahora editable desde la ficha, corrige un bug donde faltaba ese campo en `EditFichaDialog`); "Género" renombrado a "Sexo" con masculino/femenino/no binario, opciones centralizadas en `src/components/patients/sexOptions.ts` (antes duplicadas 3 veces); contacto de emergencia separado en nombre y apellido (`emergency_contact_first_name`/`last_name`); campo de alergias (texto libre) nuevo en alta y ficha. 3 migraciones aplicadas contra Supabase (normalización de género sucio `M`/`F`→`male`/`female` en 6 pacientes, columna `allergies`, columnas de contacto de emergencia con backfill automático en 7 pacientes) y `types.ts` regenerado. Verificado de punta a punta en el navegador contra Supabase real (alta completa con los 4 campos, validación de nacionalidad obligatoria, edición de un paciente viejo con datos migrados) — 0 errores de consola. Typecheck, lint (239, en el techo) y build OK. 6 commits, pusheados a main. `emergency_contact_name` queda deprecada sin borrar (candidato en TASKS.md).
- **[2026-08-11] Ejercicios: corrección — Ejercicio → Plan → Programa (3 pestañas)** (feedback del usuario sobre el cierre del 08-10, ver DECISIONS): Biblioteca reordenada en 3 pestañas de primer nivel (Ejercicios/Planes/Programas); Activo/Activo asistido/Fortalecimiento pasa a ser un filtro dentro de Ejercicios; "Rutina" renombrada a "Plan" en toda la UI (solo texto, tabla sigue siendo `exercise_routines`); nivel nuevo "Programa" reutilizable que agrupa varios Planes (`exercise_programs`/`exercise_program_routines`); wizard del paciente ahora aplica un Plan o un Programa completo; "Plan del paciente" relabeleado a "Programa del paciente" para no colisionar. 2 migraciones aplicadas por Jose y verificadas de punta a punta en el navegador con Playwright headless contra Supabase real (login real, crear Plan, crear Programa, agregarle el Plan, aplicar el Programa a un paciente de prueba hasta el paso 2 del wizard, limpieza de los datos de prueba creados) — 0 errores de consola. Typecheck, lint (239, en el techo) y build OK. 5 commits, pusheados a main.
- **[2026-08-10] Ejercicios: Rutinas reutilizables + Programa con fecha/duración** (pedido en reunión de equipo, ver DECISIONS): nivel intermedio "Rutina" (tab nueva en Biblioteca de Ejercicios, CRUD completo con reorder/dosis sugerida) entre el Ejercicio y el Programa del paciente. Wizard de 3 pasos ("Aplicar rutina") en `EjerciciosTab` para armar el plan del paciente a partir de una rutina — cantidades editables sin tocar la plantilla, más fecha de inicio + duración en semanas. Link público del paciente y guard de borrado de ejercicios actualizados. 4 migraciones (`exercise_routines`/`exercise_routine_items`, columnas de programa + `UNIQUE(patient_id)` en `exercise_plans`, RPC `add_routine_to_exercise_plan`, `get_exercise_plan_public` extendido) aplicadas por Jose y verificadas de punta a punta en el navegador (login real, crear rutina, aplicar al paciente, link público, guard de borrado — 0 errores de consola). Lint en el techo (239), typecheck y build OK. **Sin commitear todavía** (no se pidió explícitamente en la sesión).
- **[2026-07-17] Cuatro mejoras del flujo de pacientes** (20 commits, todo verificado con lint/typecheck/tests/build):
  1. **Obra social**: checkbox "No posee obra social" en alta y edición; el campo de edición ahora usa el mismo autocomplete que el alta (`InsuranceField.tsx`). Guarda el string fijo `"No posee"` (null = sin dato).
  2. **Diagnósticos múltiples**: tabla `episode_diagnoses` (N por episodio, position 0 = principal, RLS igual que fichas) con backfill de los 15 episodios existentes. Editor compartido (`DiagnosisListEditor.tsx` + helpers en `diagnoses.ts`) en alta, sesión de admisión, nuevo episodio y edición de ficha. El principal se sigue escribiendo en las columnas legacy — ver DECISIONS.
  3. **Alta y abandono**: botón "Dar de alta" (navega con `?type=discharge` preseleccionado) y "Marcar abandono" (nuevo valor de enum `abandoned` + `abandoned_at`/`abandon_reason`, cierra episodio, diálogo con motivo opcional, botón Reactivar, tab en el listado). Guards para que borrar/editar sesiones no pise el estado abandonado.
  4. **Perfil ocupacional**: situación laboral, estado civil y nivel educativo como selects con opciones predefinidas (`occupationalOptions.ts`); además se agregó el campo "gestión de la salud" que faltaba en la edición de ficha.
  Migraciones aplicadas en remoto: `20260717010000`, `020000`, `021000`, `030000`, `031000`. Suite RLS ampliada a 16 tests (verde contra el proyecto real).
- **[2026-07-16] CI + tests de RLS.** GitHub Actions corre lint (techo 260 warnings) + typecheck + tests + build en cada push/PR. Suite de 12 tests de RLS contra el Supabase real (aislamiento de pacientes/sesiones/fichas entre profesionales + anon sin acceso) con usuarios y datos de prueba fijos que se reutilizan. Hallazgo pendiente de decisión en `TASKS.md`: el registro está abierto a cualquiera (signUp crea perfil de profesional activo sin invitación).
- **[2026-07-15] Hardening de seguridad** (migración `20260715130000_security_hardening.sql`, aplicada y verificada contra la API): funciones RPC sensibles sin acceso anon, trigger functions no invocables, search_path fijo, bucket avatars sin listado público. Además `.env` fuera de git (+ `.env.example`), `.gitignore` arreglado (líneas UTF-16 rotas) y mínimo de contraseña a 8 en los 4 formularios. Pendiente bloqueado por plan Pro: leaked password protection (ver `TASKS.md`).
- **[2026-07-15] Fix recuperación de contraseña / mails de auth**: el Site URL de Supabase estaba en el default `localhost:3000` y producción no estaba en la allowlist — configurado en dashboard, verificado andando. El cambio de email ahora redirige a `/profile`.
- **[2026-07-15] Módulo Perfil en `/profile`** (ítem en sidebar debajo de Ejercicios): edición de datos personales (nombre, especialidad, matrícula), foto de avatar (bucket `avatars` público + columna `avatar_url`, con compresión client-side a 512px porque las fotos de cámara superaban el límite de 2MB del bucket), cambio de contraseña (verificando la actual) y cambio de email (flujo de confirmación de Supabase; `profiles.email` se sincroniza client-side en `fetchProfile`, sin trigger). AuthContext ahora expone `refreshProfile`. Verificado: datos personales y avatar. Sin probar todavía: flujo completo de cambio de email (requiere casilla accesible y chequear el redirect URL del mail en el dashboard de Supabase).
- **[2026-07-15] Fix: spinner infinito en `/` para usuarios sin sesión.** `useIsAdmin` dejaba `isAdmin` en `null` para siempre cuando no había sesión (a propósito — ver comentario del hook), pero `AppLayout` trataba `isAdmin === null` como "todavía cargando" sin importar si había sesión. Resultado: nunca se llegaba a evaluar la redirección a `/login`, pantalla en blanco con spinner eterno. Fix en `src/components/AppLayout.tsx`: ahora solo espera a `isAdmin` cuando efectivamente hay `session` (mismo orden que ya usaba `AdminLayout`).
- **[2026-07-15] Setup del sistema de tracking del proyecto** (este mismo archivo + `TASKS.md`, `DECISIONS.md`, `CHANGELOG.md` en `docs/`, y reglas de flujo de trabajo en `CLAUDE.md`).

## 🧠 Contexto que Claude Code necesita saber HOY
(Decisiones recientes, cosas raras del schema, cosas que no están en el código ni importan)
- **Flujo de Git desde el 2026-09-15:** `main` protegida (push directo prohibido, incluso para Jose). Todo cambio va por rama `feature/nombre` o `fix/nombre` + PR contra `main` + squash merge + borrar rama. Colabora Javito (`javiruarte97-cmd`), mismo flujo sin excepciones. Repo linkeado a Vercel (`proyec-to`): cada PR genera preview automático, merge a `main` deploya a producción. Si `git push` falla con "could not read Password", correr `gh auth setup-git` primero (gh ya está autenticado como `JoseRuarte04`).
- Ejercicios ahora es Ejercicio → Plan → Programa (3 pestañas en `/exercises`). "Plan" es el nombre nuevo de lo que en la base sigue siendo `exercise_routines` (no se renombró la tabla, solo el texto de la UI — carpeta `src/components/exercises/plans/`). "Programa" es un nivel nuevo y reutilizable que agrupa varios Planes (tablas `exercise_programs` / `exercise_program_routines`). Corregido 2026-09-18: este bullet decía "migraciones todavía sin aplicar" pero se contradecía con la entrada de Cerrado del 2026-08-11 (que ya las daba por aplicadas y verificadas) — confirmado contra Supabase real que las tablas y la columna `exercise_plans.program_id` **sí existen** en producción. El bullet viejo quedó desactualizado, no la entrada de Cerrado.
- Ninguna de las migraciones de `document_type`, `full_name` de obras sociales ni `exercise_programs` aparece con su versión real en el historial de migraciones de Supabase (`list_migrations`) — se aplicaron directo contra producción en algún momento sin que el archivo del repo quedara sincronizado a esa misma versión. No rompe nada hoy (los archivos del repo ya son idempotentes donde se detectó), pero es candidato para una limpieza dedicada — ver TASKS.md.
- Turnos virtuales usan Jitsi Meet (links generados en el cliente en `src/lib/videoRoom.ts`, sin API key ni edge function). Daily.co se probó primero y se descartó — hay una edge function `create-daily-room` huérfana deployada en Supabase (`pvuaqatdendcgumwktid`) que se puede borrar del dashboard. Caveat conocido: en `meet.jit.si` el que inicia la reunión necesita loguearse como moderador; los pacientes entran sin cuenta. Ver candidato en `TASKS.md`.
- El servidor de desarrollo local corre en el puerto **8080** (no el 5173 default de Vite) — está fijado en `vite.config.ts`.
- Los diagnósticos viven en `episode_diagnoses` (fuente de verdad) pero el principal se sincroniza a `patient_clinical_records.diagnosis` y `treatment_episodes.diagnosis` en cada save. Estadísticas futuras deben leer la tabla nueva. Obra social: `"No posee"` es un valor sentinela distinto de null.
- El lint bajó a 235 warnings desde el 2026-09-15 (se sacó código al pasar Evaluación analítica a acordeón) — el techo del CI seguía en 239, así que hay margen ahora, pero ojo con `react-refresh/only-export-components`: no exportar helpers desde archivos de componentes (por eso `performanceContextTypes.ts` está separado de `PerformanceContextSections.tsx`).
- El checkout local no tenía `.env` (solo `.env.example`) hasta el 2026-08-10 — se creó uno local (gitignorado) con la `anon key` para poder probar en el navegador. Si se clona el repo de nuevo hace falta recrearlo a mano.
- No hay borrado duro de pacientes desde la UI (solo alta/abandono) — al limpiar datos de prueba hay que borrar a mano vía SQL. Ojo: `exercise_plan_tokens.patient_id` no tiene `ON DELETE CASCADE` (a diferencia de `plan_id`), hay que borrar el `exercise_plans` del paciente antes que el paciente mismo. Candidato de limpieza anotado en `TASKS.md`.
- `patients.gender` ("Sexo" en la UI) sigue siendo `text` libre sin CHECK, opciones en `src/components/patients/sexOptions.ts` (male/female/non_binary). `patients.emergency_contact_name` está deprecada desde el 2026-09-15 (reemplazada por `emergency_contact_first_name`/`last_name`, con backfill ya aplicado) pero no se borró — candidato en `TASKS.md`. `nationality` sigue nullable en DB (obligatoria solo en el formulario de alta, no a nivel DB, porque hay pacientes viejos sin el dato).
- Perfil ocupacional (`patient_occupational_profiles`) solo tiene 5 campos con UI desde el 2026-09-15: `dominance`, `marital_status`, `education_level`, `job`, `support_network` — se edita en 2 lugares a propósito (Ficha del paciente y paso de la sesión de Admisión, `PerfilOcupacionalStep.tsx`). El resto de las columnas de la tabla (`employment_status`, `education`, `leisure`, `physical_activity`, `sleep_rest`, `health_management`, `notes`, `avd`, `aivd`, `dash_score`) quedan sin UI, deprecadas sin borrar.
- Evaluación funcional (`functional_evaluations`, paso "Eval. funcional" del wizard de sesiones, aparece en TODAS las sesiones no solo admisión) tiene desde el 2026-09-15 una estructura de 5 apartados AOTA/MOHO: apartado 1 "Ocupaciones" es un checklist de 47 ítems en 9 categorías (`src/components/evaluations/occupationsTaxonomy.ts`) guardado en la columna `occupations_items` (jsonb, clave `"<categoría>.<ítem>"` → `independent`/`assistance`/`dependent`), apartados 2-5 son texto libre (`context_*`, `performance_pattern_*`, `performance_skill_*`, `client_factor_*`). Barthel y FIM viven aparte en `FunctionalScales.tsx` (ver bullets siguientes). `NewFuncEvalDialog.tsx` (creación standalone desde la pestaña Evaluaciones) NO tiene esta estructura nueva — problema preexistente anotado en `TASKS.md`.
- Índice de Barthel (`BarthelSection`/`BarthelItemToggle` en `FunctionalScales.tsx`) desde el 2026-09-15 muestra todas las opciones de cada ítem como botones (2-4 según el ítem), no un `<select>`. FIM sigue con `<select>` a propósito (ver DECISIONS 2026-09-15) — no asumir que ambos tienen la misma UI.
- Evaluación analítica (`AnaliticaStep.tsx`, `sections_config` en `analytical_evaluations`) desde el 2026-09-15 ya NO usa switches de "activar sección" — las 8 sub-secciones son un acordeón (`SubSection` en `src/components/session/shared.tsx`, ahora usa `Collapsible` en vez de `Switch`) siempre visible, colapsado por defecto, sin persistir qué estaba abierto. `sections_config` quedó deprecada (columna sin uso nuevo).
- `fetchEpisodeDiagnoses` (`src/components/patients/diagnoses.ts`) desde el 2026-09-15 **tira el error** si la query falla (antes devolvía `[]` en silencio). Sus 3 callers (`EditFichaDialog.tsx`, `SessionForm.tsx`, `PatientProfile.tsx`) deben seguir capturándolo — los primeros dos ya tienen un flag `diagnosesLoadFailed` que evita guardar (y potencialmente borrar) diagnósticos si la carga previa falló. Si se agrega un caller nuevo, replicar ese patrón, no solo un try/catch con toast.
- Perfil ocupacional (`SessionForm.tsx`, `FichaTab.tsx`) desde el 2026-09-15 solo inserta fila/muestra card si hay al menos un campo cargado (antes insertaba filas vacías siempre que `isAdmission`) — mismo patrón que `hasFunctionalData` ya usado para `functional_evaluations`.
- "Plan de tratamiento" (`NewPlanDialog`/`EditPlanDialog`/`PlanDetailDialog`/`DeletePlanConfirm`/`PlanCardActions`, tabla `treatment_plans`) se borró del 2026-09-15 por ser código muerto (nadie lo abría desde la UI) — no confundir con el flujo activo Ejercicio→Plan→Programa (`exercise_routines`/`exercise_programs`), que es un concepto completamente distinto pese al nombre parecido. La tabla `treatment_plans`/`treatment_plan_exercises` sigue en la base (0 filas) — `Exercises.tsx` la usa como guard antes de borrar un ejercicio del catálogo, no tocar sin revisar eso.

---

## Última sesión de trabajo
**Fecha:** 2026-09-18
**Qué se hizo:** Jose pidió una revisión a fondo de los 6 PRs abiertos de Javito (#13-#18) antes de aprobar/pedir cambios/cerrar nada. La revisión encontró dos problemas estructurales: (1) las ramas #15→#16→#17 estaban apiladas, cada una conteniendo todos los commits de la anterior bajo un título que solo describía el último cambio; (2) #13/#17 y #14/#18 eran pares de trabajo duplicado sobre las mismas features (tipo de documento, obra social con nombre completo). Jose confirmó contra Supabase real que `patients.document_type` y `obras_sociales.full_name` ya estaban aplicados en producción sin que el commit correspondiente estuviera mergeado — pidió destrabar todo con esa realidad en mente, de a un paso confirmado por vez primero y después "en auto mode" (sin más confirmaciones, incluyendo el merge de los PRs). Se hizo: blindaje de ambas migraciones con `IF NOT EXISTS`/`DROP...IF EXISTS`; rebase de #16 y #17 (con aviso previo en cada PR antes del force-push); índice único case-insensitive en `obras_sociales.name` antes de mergear #14 (verificado antes que no hubiera duplicados previos); merge en orden #15→#16→#17→#14; cierre sin mergear de #13 y #18 con comentario y link al reemplazo; PR #19 nuevo para sincronizar el repo con el backfill de 30 obras sociales de #18 (también ya aplicado en producción). Auditoría completa de `PROJECT_STATE.md` contra GitHub y Supabase real: encontradas y corregidas 7 desincronizaciones (2 ya detectadas por Jose, 5 nuevas — incluida una contradicción interna del propio doc sobre `exercise_programs`, que sí está aplicado en producción pese a que un bullet decía lo contrario). Verificado: typecheck/build limpios en cada rama antes de mergear, CI verde en los 6 PRs + el nuevo #19.
**Qué quedó a medio camino:** Nada bloqueado — los 6 PRs originales quedaron resueltos (4 mergeados, 2 cerrados) y el PR de sync (#19) también mergeado. Pendiente para más adelante, no bloqueante: reconciliar el historial de migraciones de Supabase (`document_type`, `full_name`, `exercise_programs` no aparecen con su versión real ahí) — candidato en TASKS.md.
**Próxima sesión debería empezar por:** Retomar el catálogo de ejercicios HEP2go (falta que Jose corra las 3 migraciones de `docs/PLAN_catalogo_ejercicios_hep2go.md`, confirmado que siguen sin aplicar) o Recordatorios de turnos si se prioriza eso primero.

---

## Sesión anterior
**Fecha:** 2026-09-16
**Qué se hizo:** Jose pidió agregar tipo de documento al alta de paciente (no era continuación de HEP2go, que estaba "En progreso" — se confirmó pausarlo antes de arrancar, siguiendo la regla de CLAUDE.md). Dropdown de 6 tipos de documento (DNI, LC, LE, Pasaporte, Cédula extranjera, Otro) en alta y "Editar ficha", con validación de DNI (7-8 dígitos) solo cuando corresponde. Migración `document_type` escrita y aplicada directo contra Supabase — a mitad de sesión Jose autorizó puntualmente ese cambio de flujo. `types.ts` regenerado. Verificado de punta a punta en el navegador contra Supabase real — 0 errores de consola nuevos. Typecheck, lint (207, mismo baseline) y build OK.
**Qué quedó a medio camino:** PR #13 quedó sin mergear y terminó superado por #17 (ver sesión de arriba) — cerrado sin mergear el 2026-09-18.
**Próxima sesión debería empezar por:** (superado por la sesión de arriba)

---

## Dos sesiones atrás
**Fecha:** 2026-09-15 (más temprano)
**Qué se hizo:** Jose pidió mergear el PR #8 (Barthel/Eval. analítica) y el PR #9 (fix de queries con error silencioso). PR #9 tuvo conflicto de merge porque su rama se había creado antes de que el #8 se mergeara, y ambos tocaban los mismos docs de tracking — se resolvió conservando el contenido de ambos. `main` local sincronizado, ramas remotas obsoletas podadas.
**Qué quedó a medio camino:** Nada — PR #8 y PR #9 mergeados a `main`, ambas ramas borradas.
**Próxima sesión debería empezar por:** (superado por sesiones posteriores del mismo día)
